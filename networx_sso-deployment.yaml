apiVersion: apps/v1
kind: Deployment
metadata:
  name: networx-sso-deployment
  labels:
    app: networx-sso
spec:
  replicas: 3
  selector:
    matchLabels:
      app: networx-sso
  template:
    metadata:
      labels:
        app: networx-sso
    spec:
      # Add the hostAliases here
      hostAliases:
      - ip: "172.20.10.236"
        hostnames:
        - "mariadb.networx.com"
      containers:
      - name: networx-sso
        image: networx_sso:1.0.0
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "256Mi"
            cpu: "200m"
          requests:
            memory: "128Mi"
            cpu: "100m"